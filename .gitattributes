# Archived MediaWiki source and machine evidence retain exact bytes.
versions/**/*.wiki -text -whitespace
versions/**/*.tsv -whitespace

# A unified diff's blank context lines intentionally contain one space.
patches/*.patch -text -whitespace
